# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# Env
.env
.env.*
!.env.example

# Node / Flutter
node_modules/
.next/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
build/
# Binary store artifact'leri ignore — ama rehber/metadata text dosyaları repo'da kalsın
store-builds/**
!store-builds/
!store-builds/app-store-text/
!store-builds/app-store-text/**
!store-builds/play-store-text/
!store-builds/play-store-text/**
!store-builds/play-store-assets/
!store-builds/play-store-assets/*.md
!store-builds/play-store-assets/*.py
store-builds/**/*.aab
store-builds/**/*.apk
store-builds/**/*.ipa
store-builds/**/*.png
store-builds/**/*.jpg
store-builds/**/*.zip
store-builds/ios-export/
*.lock
!pubspec.lock
!poetry.lock

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# AWS CDK
cdk.out/
cdk.context.json

# Logs
*.log
logs/

# Uploads (local dev)
uploads/

# Android signing — ASLA REPO'YA GIRMEZ
**/key.properties
*.jks
*.keystore

# iOS signing — ASLA REPO'YA GIRMEZ
*.mobileprovision
*.p8
*.p12
*.cer
*.certSigningRequest
mobile/ios/certs/

# fastlane geçici çıktıları
mobile/ios/fastlane/report.xml
mobile/ios/fastlane/test_output/
